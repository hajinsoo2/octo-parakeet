name: manage-azure-policy-2d390b3b
on:
  workflow_dispatch:
    inputs:
      remarks:
        description: 'Reason for triggering the workflow run'
        required: false
        default: 'Updating Azure Policies'


## Uncomment the following if you wish to automatically trigger workflow run with every commit
#
#  push:
#    paths:
#      - 'policies/Activity_log_should_be_retained_for_at_least_one_year_5e856359-7127-422e-87a0-d555ffd271a0/**'
#      - .github/workflows/manage-azure-policy-2d390b3b.yml
jobs:
  apply-azure-policy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS_2d390b3b}}
        allow-no-subscriptions: true
    - name: Create or Update Azure Policies
      uses: azure/manage-azure-policy@v0
      with:
        paths: |
          policies/Activity_log_should_be_retained_for_at_least_one_year_5e856359-7127-422e-87a0-d555ffd271a0/**
